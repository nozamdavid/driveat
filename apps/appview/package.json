{
  "name": "@atgallery/appview",
  "version": "0.0.0-alpha.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run --exclude 'dist/**'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260826.1",
    "wrangler": "4.126.0"
  }
}
